---
phase: 05-run-analysis-cli
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/fk_quant_research_accel/store/migrations.py
  - src/fk_quant_research_accel/store/metadata.py
  - src/fk_quant_research_accel/run_analysis/__init__.py
  - src/fk_quant_research_accel/run_analysis/resolver.py
  - src/fk_quant_research_accel/run_analysis/queries.py
  - src/fk_quant_research_accel/run_analysis/formatters.py
  - src/fk_quant_research_accel/cli.py
  - tests/test_store.py
  - tests/test_run_analysis.py
autonomous: true

must_haves:
  truths:
    - "Researcher can list past runs with summary metrics (timestamp, status, scenario count, best score, median score) via `list-runs`"
    - "Runs can be filtered by --status, --from, --to, --min-score, --max-score, --git-sha, --manifest-hash"
    - "Runs are paginated with --limit (default 20) and --offset (default 0), newest first"
    - "Output auto-detects TTY for Rich table vs plain JSON; --format table|json|csv overrides"
    - "Run selectors (`latest`, `latest~N`, UUID prefix 8+ chars, full UUID) resolve to canonical batch_run_id"
    - "Schema migration v2->v3 adds manifest_hash column to batch_runs"
  artifacts:
    - path: "src/fk_quant_research_accel/store/migrations.py"
      provides: "v2->v3 migration adding manifest_hash TEXT column"
      contains: "_migrate_v2_to_v3"
    - path: "src/fk_quant_research_accel/store/metadata.py"
      provides: "Query methods: list_batch_runs, find_batch_runs_by_prefix"
      exports: ["MetadataStore"]
    - path: "src/fk_quant_research_accel/run_analysis/resolver.py"
      provides: "RunResolver translating selectors to canonical IDs"
      exports: ["resolve_run_id"]
    - path: "src/fk_quant_research_accel/run_analysis/queries.py"
      provides: "Higher-level query functions computing median_score"
      exports: ["list_runs_with_metrics"]
    - path: "src/fk_quant_research_accel/run_analysis/formatters.py"
      provides: "TTY-aware output (table/json/csv) for run listings"
      exports: ["get_effective_format", "emit_runs_table", "emit_json", "emit_csv"]
    - path: "src/fk_quant_research_accel/cli.py"
      provides: "list-runs command wired to query + format pipeline"
      contains: "list_runs"
    - path: "tests/test_run_analysis.py"
      provides: "Tests for resolver, queries, formatters, list-runs CLI"
    - path: "tests/test_store.py"
      provides: "Tests for v3 migration and new MetadataStore methods"
  key_links:
    - from: "src/fk_quant_research_accel/cli.py"
      to: "src/fk_quant_research_accel/run_analysis/queries.py"
      via: "list_runs_with_metrics call in list-runs command"
      pattern: "list_runs_with_metrics"
    - from: "src/fk_quant_research_accel/run_analysis/queries.py"
      to: "src/fk_quant_research_accel/store/metadata.py"
      via: "MetadataStore.list_batch_runs for filtered query"
      pattern: "store\\.list_batch_runs"
    - from: "src/fk_quant_research_accel/run_analysis/resolver.py"
      to: "src/fk_quant_research_accel/store/metadata.py"
      via: "MetadataStore.find_batch_runs_by_prefix and list_batch_runs"
      pattern: "store\\.(find_batch_runs_by_prefix|list_batch_runs)"
    - from: "src/fk_quant_research_accel/cli.py"
      to: "src/fk_quant_research_accel/run_analysis/formatters.py"
      via: "Format detection and output emission"
      pattern: "get_effective_format|emit_"
---

<objective>
Build the foundation layer for run analysis (schema migration, MetadataStore queries, run resolver, output formatters) and deliver the `list-runs` CLI command.

Purpose: Enables researchers to query past experiment runs with filtering, pagination, and multi-format output. Also establishes the resolver and query infrastructure that `compare-runs` and `show-run` will depend on in Plan 02.

Output: Working `list-runs` command with filters, pagination, and table/json/csv output; RunResolver for selector-to-ID translation; schema v3 with manifest_hash column.
</objective>

<execution_context>
@/Users/sakeeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakeeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-run-analysis-cli/05-RESEARCH.md
@.planning/phases/04-scoring-diagnostics-leaderboards/04-02-SUMMARY.md
@src/fk_quant_research_accel/store/migrations.py
@src/fk_quant_research_accel/store/metadata.py
@src/fk_quant_research_accel/cli.py
@src/fk_quant_research_accel/leaderboard.py
@tests/test_store.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration v3 and MetadataStore query methods (TDD)</name>
  <files>
    src/fk_quant_research_accel/store/migrations.py
    src/fk_quant_research_accel/store/metadata.py
    tests/test_store.py
  </files>
  <action>
**RED phase -- write failing tests first in `tests/test_store.py`:**

1. `test_init_db_v3_adds_manifest_hash_column`: Assert `manifest_hash` column exists in `batch_runs` after `init_db`. Assert `CURRENT_SCHEMA_VERSION == 3` and `PRAGMA user_version` returns 3.

2. `test_list_batch_runs_returns_all_unfiltered`: Create 3 batch runs with `create_batch_run` (using different `batch_run_id`, `created_at` timestamps). Call `store.list_batch_runs()` with no filters. Assert returns 3 rows in descending `created_at` order. Assert each row is a dict with `batch_run_id`, `created_at`, `status`, `scenario_count`, `best_score` keys.

3. `test_list_batch_runs_filters_by_status`: Create 2 completed and 1 running batch. Call `store.list_batch_runs(status="completed")`. Assert returns 2 rows.

4. `test_list_batch_runs_filters_by_date_range`: Create 3 runs at different dates (2025-01-01, 2025-06-15, 2025-12-31). Call `store.list_batch_runs(from_date="2025-03-01", to_date="2025-09-01")`. Assert returns 1.

5. `test_list_batch_runs_filters_by_git_sha`: Create 2 runs with different git_sha. Filter by one. Assert returns 1.

6. `test_list_batch_runs_filters_by_manifest_hash`: Create batch run, then update its manifest_hash manually. Filter by `manifest_hash`. Assert returns 1.

7. `test_list_batch_runs_pagination`: Create 5 runs. Call with `limit=2, offset=0`. Assert 2 returned. Call with `limit=2, offset=2`. Assert 2 returned. Call with `limit=2, offset=4`. Assert 1 returned.

8. `test_list_batch_runs_score_filters`: Create batch with scenarios (some completed with scores). Test `min_score` and `max_score` filters work via HAVING clause on best_score.

9. `test_find_batch_runs_by_prefix_exact`: Create a run with known ID. Call `store.find_batch_runs_by_prefix(id[:10])`. Assert returns exactly 1 match.

10. `test_find_batch_runs_by_prefix_multiple`: Create 2 runs with IDs starting with same 6 chars (generate until collision or use deterministic IDs). Call with shared prefix. Assert returns 2.

11. `test_find_batch_runs_by_prefix_no_match`: Call with nonexistent prefix. Assert returns empty list.

**GREEN phase -- implement the minimal code:**

In `migrations.py`:
- Bump `CURRENT_SCHEMA_VERSION` to `3`.
- Add `2: _migrate_v2_to_v3` entry in `_apply_migrations` migration map.
- Implement `_migrate_v2_to_v3(conn)`: Execute `ALTER TABLE batch_runs ADD COLUMN manifest_hash TEXT`. This is a nullable column -- existing rows will have NULL (acceptable per research decision).

In `metadata.py`:
- Add `list_batch_runs(self, status, from_date, to_date, git_sha, manifest_hash, min_score, max_score, order_by, limit, offset) -> list[dict[str, Any]]` method. Build dynamic SQL with WHERE conditions and parameterized queries. Use LEFT JOIN to `scenario_runs` for `best_score` (MIN of completed scores) and aggregated counts. Apply `min_score`/`max_score` as HAVING clauses on the grouped result. Allowlist `order_by` values to `{"created_at DESC", "created_at ASC"}` to prevent SQL injection. Use `GROUP BY b.batch_run_id` and `LIMIT ? OFFSET ?`.
- Add `find_batch_runs_by_prefix(self, prefix: str) -> list[dict[str, Any]]` method. Execute `SELECT * FROM batch_runs WHERE batch_run_id LIKE ? || '%'` with the prefix as parameter.

Important implementation details:
- Follow existing MetadataStore pattern: `self.connection.execute(query, params).fetchall()` returning `[dict(row) for row in rows]`.
- `min_score` and `max_score` use HAVING clause (not WHERE) since they filter on aggregate MIN(score).
- Use `b.*` alias for batch_runs and `s.*` for scenario_runs in the JOIN query.
- Default `order_by="created_at DESC"` per locked decision (newest first).

**REFACTOR:** Ensure all tests pass. Run `python3 -m pytest tests/test_store.py -q`.
  </action>
  <verify>
    `python3 -m pytest tests/test_store.py -q` -- all pass including new v3 migration and query method tests.
    `python3 -c "from fk_quant_research_accel.store.migrations import CURRENT_SCHEMA_VERSION; assert CURRENT_SCHEMA_VERSION == 3"` -- confirms version bump.
  </verify>
  <done>
    Schema v3 migration adds `manifest_hash TEXT` to `batch_runs`. `list_batch_runs` supports status/date/git_sha/manifest_hash/score filters with pagination. `find_batch_runs_by_prefix` returns rows matching ID prefix. All new tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run resolver, queries, and output formatters (TDD)</name>
  <files>
    src/fk_quant_research_accel/run_analysis/__init__.py
    src/fk_quant_research_accel/run_analysis/resolver.py
    src/fk_quant_research_accel/run_analysis/queries.py
    src/fk_quant_research_accel/run_analysis/formatters.py
    tests/test_run_analysis.py
  </files>
  <action>
Create the `run_analysis/` subpackage under `src/fk_quant_research_accel/`.

**RED phase -- write failing tests first in `tests/test_run_analysis.py`:**

**Resolver tests:**

1. `test_resolve_run_id_full_uuid`: Create a MetadataStore (tmp_path), insert a batch run with known UUID. Call `resolve_run_id(full_uuid, store)`. Assert returns that UUID.

2. `test_resolve_run_id_prefix`: Insert a batch run. Call `resolve_run_id(uuid[:10], store)`. Assert returns full UUID.

3. `test_resolve_run_id_prefix_too_short`: Call `resolve_run_id("abc", store)`. Assert raises ValueError with "at least 8 characters".

4. `test_resolve_run_id_prefix_ambiguous`: Insert 2 batch runs whose IDs share the same first 8 chars (use deterministic IDs like `"aaaaaaaa-1111-..."` and `"aaaaaaaa-2222-..."`). Call `resolve_run_id("aaaaaaaa", store)`. Assert raises ValueError with "Ambiguous".

5. `test_resolve_run_id_prefix_not_found`: Call `resolve_run_id("zzzzzzzz", store)`. Assert raises ValueError with "No run found".

6. `test_resolve_run_id_latest`: Insert 3 batch runs with ascending `created_at`. Call `resolve_run_id("latest", store)`. Assert returns the newest run's ID.

7. `test_resolve_run_id_latest_tilde_n`: Insert 3 batch runs. Call `resolve_run_id("latest~1", store)`. Assert returns the second-newest. Call `resolve_run_id("latest~2", store)`. Assert returns the oldest.

8. `test_resolve_run_id_latest_out_of_range`: Insert 1 run. Call `resolve_run_id("latest~5", store)`. Assert raises ValueError.

**Queries tests:**

9. `test_list_runs_with_metrics_adds_median`: Create a batch run with 3 completed scenarios (scores: 0.1, 0.3, 0.5). Call `list_runs_with_metrics(store)`. Assert result contains `median_score` == 0.3 for that batch. Also assert `best_score` is 0.1 (lowest = best for loss-based scoring).

10. `test_list_runs_with_metrics_no_completed_scenarios`: Create a batch with only failed scenarios. Assert `median_score` is None and `best_score` is None.

**Formatter tests:**

11. `test_get_effective_format_explicit_override`: Assert `get_effective_format("json")` returns `"json"` regardless of TTY.

12. `test_get_effective_format_none_tty`: With a Console(force_terminal=True), assert returns `"table"`.

13. `test_emit_json_output`: Call `emit_json(records)` and capture stdout. Assert valid JSON array with expected fields.

14. `test_emit_csv_output`: Call `emit_csv(records)` and capture stdout. Assert header row + data rows match expected.

15. `test_emit_runs_table_renders`: Call `emit_runs_table(records, console=Console(file=StringIO(), force_terminal=True))`. Assert no exception raised and output contains expected column headers.

**GREEN phase -- implement:**

`run_analysis/__init__.py`:
- Export: `resolve_run_id`, `list_runs_with_metrics`, `get_effective_format`.

`run_analysis/resolver.py`:
- Implement `resolve_run_id(selector: str, store: MetadataStore) -> str` as described in the research patterns section. Use `re.compile(r"^latest(?:~(\d+))?$")` for latest selector parsing. Enforce 8-char minimum prefix. Return clear error messages on ambiguous/not-found.

`run_analysis/queries.py`:
- Implement `list_runs_with_metrics(store, **filter_kwargs) -> list[dict[str, Any]]`. Call `store.list_batch_runs(**filter_kwargs)` to get batch rows. For each batch, fetch completed scenario scores via `store.get_scenario_runs(batch_run_id)`, filter to `status == "completed"` and non-None `score`, compute `statistics.median(scores)` if any exist. Attach `median_score` to each batch dict. Return enriched list.
- Use `statistics.median` from stdlib. Handle empty score list by setting `median_score = None`.

`run_analysis/formatters.py`:
- `OutputFormat = Literal["table", "json", "csv"]`
- `get_effective_format(explicit: OutputFormat | None) -> OutputFormat`: If explicit is not None, return it. Otherwise check `Console().is_terminal` -- return "table" if TTY, "json" if piped.
- `emit_runs_table(runs: list[dict], verbose: bool = False, console: Console | None = None) -> None`: Build a Rich Table with columns: Run ID (first 12 chars, cyan, no_wrap), Created (dim), Status (center), Scenarios (right), Done/Fail (right), Best Score (right, green), Median Score (right). Use `Console(stderr=True)` if no console provided (matching leaderboard.py convention). If `verbose`, add git_sha and manifest_hash columns.
- `emit_json(records: list[dict]) -> None`: `print(json.dumps(records, indent=2, default=str))` to stdout.
- `emit_csv(records: list[dict]) -> None`: Use `csv.DictWriter` with `fieldnames` from first record's keys. Write header + rows to stdout.

**REFACTOR:** Clean up, run all tests.
  </action>
  <verify>
    `python3 -m pytest tests/test_run_analysis.py -q` -- all pass.
    `python3 -c "from fk_quant_research_accel.run_analysis import resolve_run_id, list_runs_with_metrics, get_effective_format"` -- imports succeed.
  </verify>
  <done>
    RunResolver translates `latest`, `latest~N`, UUID prefix (8+ chars), and full UUID to canonical batch_run_id with clear errors on ambiguous/not-found. `list_runs_with_metrics` enriches batch rows with median_score. Formatters auto-detect TTY and emit table/json/csv. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: list-runs CLI command wiring</name>
  <files>
    src/fk_quant_research_accel/cli.py
    tests/test_cli.py
  </files>
  <action>
Wire the `list-runs` command into the existing Typer app in `cli.py`.

Add import at top of `cli.py`:
```python
from .run_analysis.queries import list_runs_with_metrics
from .run_analysis.formatters import get_effective_format, emit_runs_table, emit_json, emit_csv
from .store.metadata import MetadataStore
```

Add command:
```python
@app.command("list-runs")
def list_runs_command(
    db_path: str = typer.Option("artifacts/experiments.db", "--db-path"),
    status: str | None = typer.Option(None, "--status", help="Filter by run status"),
    from_date: str | None = typer.Option(None, "--from", help="Start date (ISO format)"),
    to_date: str | None = typer.Option(None, "--to", help="End date (ISO format)"),
    min_score: float | None = typer.Option(None, "--min-score", help="Min best_score"),
    max_score: float | None = typer.Option(None, "--max-score", help="Max best_score"),
    git_sha: str | None = typer.Option(None, "--git-sha", help="Filter by git SHA"),
    manifest_hash: str | None = typer.Option(None, "--manifest-hash", help="Filter by manifest hash"),
    limit: int = typer.Option(20, "--limit", min=1, help="Max runs to return"),
    offset: int = typer.Option(0, "--offset", min=0, help="Skip first N runs"),
    output_format: str | None = typer.Option(None, "--format", help="Output format: table|json|csv"),
    verbose: bool = typer.Option(False, "--verbose", help="Show additional columns"),
) -> None:
```

Implementation:
1. Open `MetadataStore(db_path)` -- handle missing DB file gracefully with `typer.Exit(code=1)` and error log.
2. Call `list_runs_with_metrics(store, status=status, from_date=from_date, to_date=to_date, min_score=min_score, max_score=max_score, git_sha=git_sha, manifest_hash=manifest_hash, limit=limit, offset=offset)`.
3. Determine format via `get_effective_format(output_format)`.
4. Dispatch to `emit_runs_table(runs, verbose=verbose)`, `emit_json(runs)`, or `emit_csv(runs)`.
5. Close store.

**Tests in `tests/test_cli.py`:**

Add tests using `CliRunner` and a real tmp_path SQLite database:

1. `test_list_runs_empty_db`: Create empty DB at tmp_path. Invoke `list-runs --db-path {tmp_db} --format json`. Assert exit code 0 and JSON output is `[]`.

2. `test_list_runs_with_data`: Create DB, insert 2 batch runs with scenario data. Invoke `list-runs --db-path {tmp_db} --format json`. Assert exit code 0 and JSON contains 2 entries with expected fields.

3. `test_list_runs_status_filter`: Insert 1 completed and 1 running batch. Invoke `list-runs --db-path {tmp_db} --status completed --format json`. Assert returns 1 entry.

4. `test_list_runs_pagination`: Insert 5 runs. Invoke with `--limit 2 --format json`. Assert returns 2.

5. `test_list_runs_table_format`: Invoke with `--format table`. Assert exit code 0 (table renders to stderr, so don't assert stdout content -- just confirm no crash).

6. `test_list_runs_csv_format`: Invoke with `--format csv`. Assert exit code 0 and stdout contains CSV header with expected column names.

For CLI tests, create a helper fixture that builds a pre-populated test DB:
```python
@pytest.fixture
def populated_db(tmp_path):
    db_path = tmp_path / "experiments.db"
    store = MetadataStore(db_path)
    # Insert test data...
    store.close()
    return str(db_path)
```

Run: `python3 -m pytest tests/test_cli.py -q` -- all pass.
Run: `python3 -m pytest tests/ -q` -- full suite passes.
  </action>
  <verify>
    `python3 -m pytest tests/test_cli.py -q` -- all pass.
    `python3 -m pytest tests/ -q` -- full suite passes with no regressions.
    `python3 -m fk_quant_research_accel.cli list-runs --help` -- shows all expected options.
  </verify>
  <done>
    `list-runs` CLI command works with all locked-decision filters (--status, --from, --to, --min-score, --max-score, --git-sha, --manifest-hash), pagination (--limit, --offset), and output format override (--format table|json|csv). Default sort is newest first. All CLI tests pass. Full test suite passes.
  </done>
</task>

</tasks>

<verification>
1. `python3 -m pytest tests/ -q` -- full suite passes (all existing + new tests).
2. `python3 -m fk_quant_research_accel.cli list-runs --help` -- shows all filter, pagination, and format options.
3. `python3 -c "from fk_quant_research_accel.store.migrations import CURRENT_SCHEMA_VERSION; assert CURRENT_SCHEMA_VERSION == 3"` -- schema bumped.
4. `python3 -c "from fk_quant_research_accel.run_analysis import resolve_run_id, list_runs_with_metrics, get_effective_format"` -- all imports work.
5. `ruff check src/fk_quant_research_accel/` -- no lint errors.
</verification>

<success_criteria>
- Schema v3 migration adds manifest_hash column with backward compatibility (NULL for old runs).
- MetadataStore supports filtered+paginated batch run listing and prefix-based run lookup.
- RunResolver translates latest, latest~N, UUID prefix (8+), and full UUID to canonical IDs.
- list-runs command produces Rich table on TTY, JSON/CSV when piped or overridden.
- All filter options from locked decisions are functional.
- All tests pass with no regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-run-analysis-cli/05-01-SUMMARY.md`
</output>
