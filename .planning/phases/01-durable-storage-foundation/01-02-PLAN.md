---
phase: 01-durable-storage-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/fk_quant_research_accel/logging.py
  - src/fk_quant_research_accel/cli.py
  - src/fk_quant_research_accel/__init__.py
  - tests/test_logging.py
  - tests/test_cli.py
autonomous: true

must_haves:
  truths:
    - "structlog is configured at application startup with the CLI-specified log level"
    - "All print() statements in cli.py are replaced with structlog calls or Typer/Rich output"
    - "CLI accepts --log-level flag with DEBUG/INFO/WARNING/ERROR choices"
    - "Typer replaces argparse for CLI framework with run-batch subcommand preserved"
    - "Existing CLI behavior (run-batch with all current flags) is preserved"
  artifacts:
    - path: "src/fk_quant_research_accel/logging.py"
      provides: "structlog configuration with configurable log level"
      exports: ["configure_logging"]
    - path: "src/fk_quant_research_accel/cli.py"
      provides: "Typer-based CLI with --log-level and run-batch command"
      exports: ["app", "main"]
  key_links:
    - from: "src/fk_quant_research_accel/cli.py"
      to: "src/fk_quant_research_accel/logging.py"
      via: "configure_logging() called in @app.callback()"
      pattern: "configure_logging\\("
    - from: "src/fk_quant_research_accel/cli.py"
      to: "src/fk_quant_research_accel/models/enums.py"
      via: "LogLevel enum used for --log-level parameter"
      pattern: "LogLevel"
---

<objective>
Migrate the CLI from argparse to Typer, replace all print() statements with structured logging via structlog, and add the global --log-level flag.

Purpose: Structured logging is required by RSLT-03 and RSLT-04. Every platform output must be queryable and filterable. Typer provides a cleaner CLI framework with type-hint-driven parameters and automatic Rich integration.

Output: `logging.py` module with `configure_logging()` function. Rewritten `cli.py` using Typer with `@app.callback()` for `--log-level`. Zero `print()` calls remaining in the codebase. Tests proving logging configuration works and CLI accepts all expected flags.
</objective>

<execution_context>
@/Users/sakeeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakeeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-durable-storage-foundation/01-RESEARCH.md
@.planning/phases/01-durable-storage-foundation/01-01-SUMMARY.md
@src/fk_quant_research_accel/cli.py
@src/fk_quant_research_accel/orchestrator.py
@src/fk_quant_research_accel/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create structlog configuration module</name>
  <files>
    src/fk_quant_research_accel/logging.py
    tests/test_logging.py
  </files>
  <action>
    1. Create `src/fk_quant_research_accel/logging.py`:
       - `configure_logging(log_level: str = "INFO") -> None`: configure structlog with the specified level.
       - Use `structlog.make_filtering_bound_logger(numeric_level)` as wrapper_class for fast upfront filtering.
       - Processor pipeline (in order): `structlog.contextvars.merge_contextvars`, `structlog.processors.add_log_level`, `structlog.processors.TimeStamper(fmt="iso", utc=True)`, `structlog.processors.StackInfoRenderer()`, `structlog.processors.format_exc_info`, `structlog.processors.UnicodeDecoder()`, `structlog.processors.JSONRenderer()`.
       - Use `structlog.PrintLoggerFactory(file=sys.stderr)` as logger_factory (logs go to stderr, not stdout, so they don't interfere with CLI output).
       - Set `cache_logger_on_first_use=True` for performance.
       - Set `context_class=dict`.
       - Convert level_name to numeric using `getattr(logging, log_level.upper(), logging.INFO)`.
       - Follow research Pattern 3 exactly.

    2. Create `tests/test_logging.py`:
       - Test `configure_logging("INFO")` does not raise and structlog.get_logger() works after.
       - Test `configure_logging("DEBUG")` enables debug-level messages: call `log.debug("test")` and capture stderr output.
       - Test `configure_logging("ERROR")` filters out info-level: call `log.info("should_not_appear")` and verify it does NOT appear in stderr.
       - Test that bound logger preserves context: `log.bind(run_id="abc").info("test")` includes `run_id` in output.
       - Use `capsys` or `capfd` fixture for capturing stderr output (structlog writes to stderr via PrintLoggerFactory). Note: structlog with `cache_logger_on_first_use=True` may need `structlog.reset_defaults()` between tests to avoid caching issues. Call `structlog.reset_defaults()` in a fixture.

    Run: `pytest tests/test_logging.py -v`
  </action>
  <verify>
    `pytest tests/test_logging.py -v` passes all tests.
    `python -c "from fk_quant_research_accel.logging import configure_logging; configure_logging('DEBUG')"` does not raise.
    `ruff check src/fk_quant_research_accel/logging.py` reports no lint errors.
  </verify>
  <done>
    structlog is configured with JSON output to stderr, configurable log level via string parameter, contextvars support for bound context, and ISO timestamps. Tests confirm level filtering works (DEBUG messages appear at DEBUG level, are filtered at ERROR level) and bound context variables are preserved in output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate CLI from argparse to Typer with --log-level support</name>
  <files>
    src/fk_quant_research_accel/cli.py
    src/fk_quant_research_accel/__init__.py
    tests/test_cli.py
  </files>
  <action>
    1. Rewrite `src/fk_quant_research_accel/cli.py`:
       - Replace `import argparse` with `import typer`.
       - Create `app = typer.Typer(name="fk-research", help="FK Quant Research Acceleration Platform")`.
       - Add `@app.callback()` function that accepts `--log-level` as `LogLevel` enum (import from `models.enums`). Call `configure_logging(log_level.value)` inside the callback. Follow research Pattern 4.
       - Convert the `run-batch` subcommand to `@app.command("run-batch")` function. Preserve ALL existing CLI flags with their defaults:
         - `--base-url` (required, str)
         - `--dimensions` (default "5,10", str)
         - `--volatilities` (default "0.15,0.2", str)
         - `--correlations` (default "0.0,0.3", str)
         - `--option-types` (default "call", str)
         - `--n-steps` (default 40, int)
         - `--batch-size` (default 64, int)
         - `--n-mc-paths` (default 256, int)
         - `--learning-rate` (default 1e-3, float)
         - `--poll-seconds` (default 1.5, float)
         - `--max-wait-seconds` (default 1800.0, float)
         - `--output` (default "artifacts/batch_results.csv", str)
       - Replace the `_print_top()` function: use structlog for the leaderboard display instead of print(). Log each top scenario as a structured event: `log.info("top_scenario", rank=idx+1, score=row["score"], dim=row["dim"], ...)`.
       - Replace `print(f"Wrote {len(rows)} rows to {output}")` with `log.info("batch_complete", rows=len(rows), output=str(output))`.
       - The `main()` function should call `app()` (Typer's entry point). Keep the `if __name__ == "__main__"` block.
       - Keep the `_parse_int_list()` and `_parse_float_list()` helper functions (they're still needed for parsing comma-separated values from CLI strings).

    2. Update `src/fk_quant_research_accel/__init__.py`:
       - Keep existing exports. Do NOT add cli or logging to __all__ (they're application-level, not library-level).

    3. Create `tests/test_cli.py`:
       - Use `typer.testing.CliRunner` to test the CLI.
       - Test `--help` output includes "fk-research" and "--log-level".
       - Test `run-batch --help` output includes all expected flags (--base-url, --dimensions, etc.).
       - Test that `--log-level DEBUG` is accepted without error (use `--help` after to avoid actually running a batch).
       - Test that invalid log level (e.g., `--log-level INVALID`) produces an error.
       - Do NOT test actual batch execution (requires FK backend) -- only test CLI argument parsing and help output.

    Run: `pytest tests/test_cli.py -v`

    After all tests pass, do a final scan: `grep -rn "print(" src/fk_quant_research_accel/` to confirm zero print() statements remain. If any remain, replace them with structlog calls.
  </action>
  <verify>
    `pytest tests/test_cli.py -v` passes all tests.
    `pytest tests/ -v` passes ALL tests (existing + new).
    `grep -rn "print(" src/fk_quant_research_accel/` returns NO results (zero print statements).
    `ruff check src/fk_quant_research_accel/cli.py` reports no lint errors.
    `python -c "from fk_quant_research_accel.cli import app"` imports without error.
  </verify>
  <done>
    CLI uses Typer with --log-level flag accepting DEBUG/INFO/WARNING/ERROR. run-batch subcommand preserves all existing CLI flags and defaults. All print() statements replaced with structlog structured logging calls. CLI tests pass using Typer's CliRunner. Existing test suite still passes.
  </done>
</task>

</tasks>

<verification>
Run the full test suite:
```bash
cd /Users/sakeeb/Code\ repositories/feynman-kac-quant-research-acceleration
pytest tests/ -v
```

Verify zero print statements:
```bash
grep -rn "print(" src/fk_quant_research_accel/
```

Verify CLI help:
```bash
python -m fk_quant_research_accel.cli --help
python -m fk_quant_research_accel.cli run-batch --help
```

Verify log level works:
```bash
python -m fk_quant_research_accel.cli --log-level DEBUG --help
```
</verification>

<success_criteria>
1. `pytest tests/ -v` passes all tests (existing + test_logging.py + test_cli.py)
2. Zero `print()` calls remain in `src/fk_quant_research_accel/`
3. `--log-level DEBUG|INFO|WARNING|ERROR` flag works on CLI
4. structlog outputs JSON to stderr with ISO timestamps and bound context
5. Typer CLI preserves all existing run-batch flags and defaults
6. `ruff check` passes on all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/01-durable-storage-foundation/01-02-SUMMARY.md`
</output>
