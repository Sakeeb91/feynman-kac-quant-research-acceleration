---
phase: 02-yaml-manifests-validation-and-domain-models
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/fk_quant_research_accel/models/enums.py
  - src/fk_quant_research_accel/models/experiment.py
  - src/fk_quant_research_accel/models/hashing.py
  - src/fk_quant_research_accel/models/result.py
  - src/fk_quant_research_accel/models/__init__.py
  - tests/test_experiment_manifest.py
  - tests/test_result_schema.py
autonomous: true

must_haves:
  truths:
    - "ExperimentManifest can be loaded from a dict via model_validate() and all fields are accessible"
    - "Identical manifests produce the same content hash; changing any field produces a different hash"
    - "Model sweep config (architectures, hidden_sizes, activations, optimizers) is a first-class axis in the manifest"
    - "CompletedScenarioResult requires train_loss, grad_norm, runtime_seconds, error_stats, rank_score; missing any raises ValidationError"
    - "FailedScenarioResult requires error_message and status='failed'; metrics are optional"
    - "validate_and_build_result dispatches on status and returns the correct typed result or raises on malformed data"
  artifacts:
    - path: "src/fk_quant_research_accel/models/experiment.py"
      provides: "ExperimentManifest, ScenarioGridConfig, ModelSweepConfig, BatchRunConfig, ScoringConfig, OutputConfig"
      exports: ["ExperimentManifest", "ScenarioGridConfig", "ModelSweepConfig", "BatchRunConfig", "ScoringConfig", "OutputConfig", "load_manifest"]
    - path: "src/fk_quant_research_accel/models/hashing.py"
      provides: "Deterministic content hashing for manifest versioning"
      exports: ["content_hash"]
    - path: "src/fk_quant_research_accel/models/result.py"
      provides: "Strict result schemas with discriminated union"
      exports: ["CompletedScenarioResult", "FailedScenarioResult", "ErrorStats", "validate_and_build_result"]
    - path: "tests/test_experiment_manifest.py"
      provides: "TDD tests for manifest loading, validation, and hashing"
    - path: "tests/test_result_schema.py"
      provides: "TDD tests for result schema validation and dispatch"
  key_links:
    - from: "src/fk_quant_research_accel/models/hashing.py"
      to: "src/fk_quant_research_accel/models/experiment.py"
      via: "content_hash(manifest) calls manifest.model_dump(mode='json')"
      pattern: "model_dump\\(mode=\"json\"\\)"
    - from: "src/fk_quant_research_accel/models/result.py"
      to: "src/fk_quant_research_accel/models/enums.py"
      via: "ScenarioStatus used in Literal type for status field"
      pattern: "Literal\\[.*completed.*\\]"
---

<objective>
Define the researcher-facing ExperimentManifest schema hierarchy (CONF-01, CONF-07), deterministic content hashing (CONF-03), and strict result schemas with discriminated union dispatch (RSLT-01, RSLT-02).

Purpose: These are the pure domain models that all downstream Phase 2 work depends on -- validation, CLI, and orchestrator integration all consume these types. Getting them right with TDD ensures a stable foundation.

Output: Frozen Pydantic v2 models for experiment manifests and results, content hashing utility, comprehensive test suite.
</objective>

<execution_context>
@/Users/sakeeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakeeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-yaml-manifests-validation-and-domain-models/02-RESEARCH.md
@.planning/phases/01-durable-storage-foundation/01-01-SUMMARY.md
@src/fk_quant_research_accel/models/__init__.py
@src/fk_quant_research_accel/models/enums.py
@src/fk_quant_research_accel/models/manifest.py
@src/fk_quant_research_accel/models/result.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExperimentManifest schema hierarchy + content hashing + enum extensions</name>
  <files>
    src/fk_quant_research_accel/models/enums.py
    src/fk_quant_research_accel/models/experiment.py
    src/fk_quant_research_accel/models/hashing.py
    src/fk_quant_research_accel/models/__init__.py
    tests/test_experiment_manifest.py
  </files>
  <action>
    **RED phase first:** Write `tests/test_experiment_manifest.py` with failing tests, then implement.

    **Tests to write (RED):**
    1. `test_load_manifest_from_dict` -- model_validate a minimal valid dict, assert all fields accessible
    2. `test_load_manifest_from_yaml_file` -- write a YAML file to tmp, load_manifest(), assert fields
    3. `test_manifest_requires_backend_url` -- missing backend_url raises ValidationError
    4. `test_manifest_requires_scenario_grid` -- missing scenario_grid raises ValidationError
    5. `test_manifest_defaults` -- verify default values for batch_config, scoring, output, model_sweep
    6. `test_manifest_frozen` -- assigning to a field raises TypeError/ValidationError
    7. `test_content_hash_deterministic` -- same manifest produces same hash across two calls
    8. `test_content_hash_changes_on_any_field` -- change name, volatility, architecture; each produces different hash
    9. `test_content_hash_ignores_yaml_formatting` -- two YAML files with different formatting but same content produce same hash
    10. `test_model_sweep_config` -- verify architectures, hidden_sizes, activations, optimizers fields
    11. `test_dimensions_must_be_positive` -- dim=0 or dim=-1 raises ValidationError
    12. `test_volatilities_must_be_in_range` -- vol=0.0 and vol=6.0 raise ValidationError
    13. `test_manifest_version_field` -- manifest has manifest_version field defaulting to 1

    **Implementation (GREEN):**

    1. **Extend `enums.py`:** Add `OptionType(str, Enum)` with values `call`, `put`, `asian_call`, `barrier_up_and_out`. Add `ScoringStrategy(str, Enum)` with `loss_based`, `convergence_rate`, `pareto_multi_objective`.

    2. **Create `models/experiment.py`:** Define frozen Pydantic v2 model hierarchy:
       - `ScenarioGridConfig(BaseModel, frozen=True)`: `dimensions: list[int]` (min_length=1), `volatilities: list[float]` (min_length=1), `correlations: list[float] | list[list[float]]` (min_length=1), `option_types: list[str]` (default `["call"]`). Add `@field_validator("dimensions")` for positive check, `@field_validator("volatilities")` for (0.0, 5.0] range.
       - `ModelSweepConfig(BaseModel, frozen=True)`: `architectures: list[str]` (default `["default"]`), `hidden_sizes: list[list[int]] | None = None`, `activations: list[str] | None = None`, `optimizers: list[str] | None = None`.
       - `BatchRunConfig(BaseModel, frozen=True)`: `n_steps: int = Field(default=40, gt=0)`, `batch_size: int = Field(default=64, gt=0)`, `n_mc_paths: int = Field(default=256, gt=0)`, `learning_rate: float = Field(default=1e-3, gt=0.0)`, `poll_seconds: float = Field(default=1.5, gt=0.0)`, `max_wait_seconds: float = Field(default=1800.0, gt=0.0)`.
       - `ScoringConfig(BaseModel, frozen=True)`: `strategy: str = "loss_based"`, `grad_norm_weight: float = Field(default=0.01, ge=0.0)`.
       - `OutputConfig(BaseModel, frozen=True)`: `artifacts_dir: str = "artifacts"`, `db_path: str | None = None`.
       - `ExperimentManifest(BaseModel, frozen=True)`: `manifest_version: int = 1`, `name: str | None = None`, `description: str | None = None`, `problem_id: str = "black_scholes"`, `backend_url: str`, `seed: int | None = None`, `scenario_grid: ScenarioGridConfig`, `model_sweep: ModelSweepConfig = Field(default_factory=ModelSweepConfig)`, `batch_config: BatchRunConfig = Field(default_factory=BatchRunConfig)`, `scoring: ScoringConfig = Field(default_factory=ScoringConfig)`, `output: OutputConfig = Field(default_factory=OutputConfig)`.
       - `load_manifest(path: str | Path) -> ExperimentManifest`: opens file, `yaml.safe_load()`, validates empty/non-dict, calls `ExperimentManifest.model_validate(raw)`. Raises `ValueError` with file path context on failures.

    3. **Create `models/hashing.py`:** Single function `content_hash(manifest: ExperimentManifest) -> str` using `json.dumps(manifest.model_dump(mode="json"), sort_keys=True, separators=(",", ":"), ensure_ascii=True)` piped to `hashlib.sha256().hexdigest()`.

    4. **Update `models/__init__.py`:** Export the new types: `ExperimentManifest`, `ScenarioGridConfig`, `ModelSweepConfig`, `BatchRunConfig` (rename to avoid clash with orchestrator's `BatchConfig` -- use `ManifestBatchConfig` or keep as `BatchRunConfig`), `ScoringConfig`, `OutputConfig`, `load_manifest`, `content_hash`, `OptionType`, `ScoringStrategy`.

    **REFACTOR:** Ensure all tests pass, ruff check clean, mypy clean on new files.
  </action>
  <verify>
    - `pytest tests/test_experiment_manifest.py -v` -- all tests pass
    - `ruff check src/fk_quant_research_accel/models/experiment.py src/fk_quant_research_accel/models/hashing.py src/fk_quant_research_accel/models/enums.py tests/test_experiment_manifest.py`
    - `mypy src/fk_quant_research_accel/models/experiment.py src/fk_quant_research_accel/models/hashing.py`
    - `python -c "from fk_quant_research_accel.models import ExperimentManifest, content_hash, load_manifest"`
  </verify>
  <done>
    ExperimentManifest loads from dict and YAML with full validation, content_hash is deterministic, model sweep is first-class, field constraints are enforced, all tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Strict result schema with discriminated union and validation dispatch</name>
  <files>
    src/fk_quant_research_accel/models/result.py
    src/fk_quant_research_accel/models/__init__.py
    tests/test_result_schema.py
  </files>
  <action>
    **RED phase first:** Write `tests/test_result_schema.py` with failing tests, then implement.

    **Tests to write (RED):**
    1. `test_completed_result_requires_all_metrics` -- status="completed" with missing train_loss raises ValidationError
    2. `test_completed_result_requires_grad_norm` -- status="completed" with missing grad_norm raises ValidationError
    3. `test_completed_result_requires_runtime_seconds` -- missing runtime_seconds raises ValidationError
    4. `test_completed_result_requires_rank_score` -- missing rank_score raises ValidationError
    5. `test_completed_result_accepts_full_payload` -- full valid payload creates CompletedScenarioResult
    6. `test_completed_result_error_stats_optional_fields` -- ErrorStats with all None sub-fields is valid
    7. `test_failed_result_requires_error_message` -- status="failed" with missing error_message raises ValidationError
    8. `test_failed_result_metrics_optional` -- failed result without train_loss/grad_norm is valid
    9. `test_validate_and_build_result_dispatches_completed` -- returns CompletedScenarioResult for status="completed"
    10. `test_validate_and_build_result_dispatches_failed` -- returns FailedScenarioResult for status="failed"
    11. `test_validate_and_build_result_rejects_unknown_status` -- status="unknown" raises ValueError
    12. `test_validate_and_build_result_rejects_malformed_completed` -- status="completed" with missing fields raises ValidationError
    13. `test_backward_compat_scenario_result_still_works` -- existing ScenarioResult model still importable and functional (no breaking change)

    **Implementation (GREEN):**

    1. **Add `ErrorStats` model to `result.py`:** `ErrorStats(BaseModel, frozen=True)` with `pde_residual: float | None = None`, `boundary_error: float | None = None`, `relative_l2_error: float | None = None`.

    2. **Add `CompletedScenarioResult` to `result.py`:** `CompletedScenarioResult(BaseModel, frozen=True)` with required fields: `status: Literal["completed"]`, `scenario_run_id: str`, `batch_run_id: str`, `simulation_id: str`, `scenario_params: dict[str, Any]`, `train_loss: float`, `grad_norm: float`, `runtime_seconds: float`, `rank_score: float`. Optional/defaulted: `error_stats: ErrorStats = Field(default_factory=ErrorStats)`, `val_loss: float | None = None`, `lr: float | None = None`, `progress: float = 1.0`, `checkpoint_path: str | None = None`, `extra_metrics: dict[str, Any] = Field(default_factory=dict)`.

    3. **Add `FailedScenarioResult` to `result.py`:** `FailedScenarioResult(BaseModel, frozen=True)` with: `status: Literal["failed"]`, `scenario_run_id: str`, `batch_run_id: str`, `simulation_id: str | None = None`, `scenario_params: dict[str, Any]`, `error_message: str`, `runtime_seconds: float = 0.0`, `rank_score: float = float("inf")`, `extra_metrics: dict[str, Any] = Field(default_factory=dict)`.

    4. **Add `validate_and_build_result` function to `result.py`:** Takes `raw: dict[str, Any]`, dispatches on `raw.get("status")`. Returns `CompletedScenarioResult | FailedScenarioResult`. Raises `ValueError` for unknown/missing status. Lets Pydantic `ValidationError` propagate for malformed data.

    5. **Preserve existing `ScenarioResult`:** Do NOT delete it. It's used by the orchestrator and store. The new strict models are for Phase 2+ validation. Add a comment marking `ScenarioResult` as the legacy/internal model.

    6. **Update `models/__init__.py`:** Export `ErrorStats`, `CompletedScenarioResult`, `FailedScenarioResult`, `validate_and_build_result`.

    **REFACTOR:** Ensure all tests pass, ruff clean, mypy clean.
  </action>
  <verify>
    - `pytest tests/test_result_schema.py -v` -- all tests pass
    - `pytest tests/test_models.py -v` -- existing model tests still pass (backward compat)
    - `ruff check src/fk_quant_research_accel/models/result.py tests/test_result_schema.py`
    - `mypy src/fk_quant_research_accel/models/result.py`
    - `python -c "from fk_quant_research_accel.models import ScenarioResult, CompletedScenarioResult, FailedScenarioResult, validate_and_build_result"`
  </verify>
  <done>
    CompletedScenarioResult enforces all required fields (RSLT-01), FailedScenarioResult requires error_message, validate_and_build_result dispatches correctly and rejects malformed data (RSLT-02), existing ScenarioResult preserved for backward compatibility.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_experiment_manifest.py tests/test_result_schema.py tests/test_models.py -v` -- all tests pass
- `ruff check src/fk_quant_research_accel/models/ tests/test_experiment_manifest.py tests/test_result_schema.py` -- no lint errors
- `mypy src/fk_quant_research_accel/models/` -- no type errors
- `python -c "from fk_quant_research_accel.models import ExperimentManifest, content_hash, CompletedScenarioResult, FailedScenarioResult, validate_and_build_result"` -- imports succeed
</verification>

<success_criteria>
- ExperimentManifest can be loaded from YAML and validated with Pydantic v2
- Content hashing is deterministic (identical manifests = same hash, any change = different hash)
- Model sweep is a first-class axis in the manifest schema
- CompletedScenarioResult enforces all RSLT-01 required fields
- validate_and_build_result rejects malformed results with clear Pydantic errors
- All existing tests continue to pass (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/02-yaml-manifests-validation-and-domain-models/02-01-SUMMARY.md`
</output>
